export type SystemOptions = {
    systemGuildIds: string[];
    systemMessageChannelId: string;
    systemUserId: string;
};

export type Emojis = {
    error: string;
    success: string;
    previous: string;
    pause: string;
    next: string;
    stop: string;
};
